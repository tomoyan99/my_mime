import MimeComponent from "../../types/MimeComponent";
type SMimeParams = {
    certificatePath : string; // 証明書のパス
    content : MimeComponent.MimeMultipart
}

class SMime {
    constructor(params:SMimeParams) {

    }
}